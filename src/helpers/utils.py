# to store the common data here, meaning common headers, common read data from excel, csv, yaml, json


def common_headers_json():
    headers = {
        "Content_type": "application/json"
    }
    return headers

def common_headers_xml():
    headers = {
        "Content_type": "application/xml"
    }
    return headers
    
